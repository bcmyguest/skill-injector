---
name: uv-setup
description: Bootstrap a new Python project with uv the right way — uv init, packaged layout, a pinned Python version, a type checker, ruff + pre-commit hooks, and a multi-stage Dockerfile for services. Use when starting or scaffolding a new Python project.
keywords: [python, uv, bootstrap, scaffold, pyproject]
---

# Set up a new Python project with uv

Initialize a packaged Python project with uv: pin the interpreter, wire ruff and a
type checker, install pre-commit hooks, and leave build/test/docs green.
