---
name: react-ts-setup
description: Scaffold a new React + TypeScript frontend repo with Vite — ESLint + Prettier, Vitest + Testing Library, husky + lint-staged hooks, CI, and a dev Dockerfile. Use when starting a new React, TypeScript, or Vite single-page web app.
keywords: [react, typescript, vite, frontend, scaffold]
---

# Scaffold a React + TypeScript app with Vite

Bootstrap a single-page web app: Vite + React + TypeScript, lint/format/test
tooling, git hooks, and CI, with the Node version kept in lockstep everywhere.
