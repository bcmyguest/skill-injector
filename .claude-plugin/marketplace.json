{
  "name": "skill-inject",
  "owner": {
    "name": "Brandon Guest"
  },
  "metadata": {
    "description": "Local, model-agnostic automatic skill injection for Claude Code and opencode",
    "version": "0.1.1"
  },
  "plugins": [
    {
      "name": "skill-inject",
      "description": "Local, model-agnostic automatic skill injection: embeds the prompt locally, ranks it against skill descriptions, and injects the matched skill via UserPromptSubmit/PostToolUse/SessionStart hooks (ski). Requires the `ski` binary — cargo install --path .",
      "source": ".",
      "strict": false
    }
  ]
}
